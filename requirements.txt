# Pinned from a clean python 3.11 resolution, 2026-08-06. See DEF-12.
# Regenerate with: python3.11 -m venv .venv && .venv/bin/pip install <pkg> && .venv/bin/pip freeze

# Core
httpx==0.28.1
python-dotenv==1.2.2
pydantic==2.13.4

# PDF extraction
pymupdf==1.28.0

# HTML parsing
beautifulsoup4==4.15.0
lxml==6.1.1

# LLM (Gemini — primary)
google-generativeai==0.8.6

# LLM comparison experiment (Sprint 5 only — Groq)
groq==1.6.0

# PDF report generation
reportlab==5.0.0
# weasyprint  # alternative to reportlab — pick one in Sprint 4

# Demo UI
gradio==6.22.0
